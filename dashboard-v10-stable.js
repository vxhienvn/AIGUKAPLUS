function dashboardHtml() {
  return `<!doctype html>
<html lang="vi">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>AIGUKA Báo cáo V10</title>
<style>
:root{font-family:Inter,Arial,sans-serif;color:#172033;background:#f4f7fb}*{box-sizing:border-box}body{margin:0}.top{height:58px;background:#fff;border-bottom:1px solid #d8e0ec;display:flex;align-items:center;justify-content:space-between;padding:0 18px;position:sticky;top:0;z-index:20}.brand{font-weight:800;font-size:19px}.tabs{display:flex;gap:8px}.tabs a,.btn{border:1px solid #cbd5e1;background:#fff;color:#24324a;border-radius:8px;padding:9px 12px;text-decoration:none;font-weight:650;cursor:pointer}.tabs a.active,.btn.primary{background:#155eef;color:#fff;border-color:#155eef}.btn:disabled{opacity:.45;cursor:not-allowed}.wrap{padding:16px}.panel{background:#fff;border:1px solid #d7dfeb;border-radius:12px;padding:12px;margin-bottom:12px;box-shadow:0 1px 2px #0f172a0a}.filters{display:grid;grid-template-columns:repeat(4,minmax(180px,1fr));gap:10px}.field label{display:block;font-size:12px;font-weight:700;margin-bottom:5px}.field input,.field select{width:100%;height:38px;border:1px solid #cbd5e1;border-radius:7px;padding:0 10px;background:#fff}.filter-actions{display:flex;gap:7px;align-items:end;flex-wrap:wrap;margin-top:10px}.quick{display:flex;gap:6px;flex-wrap:wrap}.status{position:fixed;right:14px;bottom:14px;border-radius:999px;padding:9px 13px;color:#fff;background:#b54708;font-weight:750;z-index:50;box-shadow:0 8px 24px #0003}.status.ok{background:#067647}.status.bad{background:#b42318}.cards{display:grid;grid-template-columns:repeat(5,minmax(150px,1fr));gap:10px;margin-bottom:12px}.card{background:#fff;border:1px solid #d7dfeb;border-top:3px solid #155eef;border-radius:10px;padding:12px}.card .label{font-size:12px;color:#475467}.card .value{font-size:25px;font-weight:850;margin-top:8px}.card .hint{font-size:11px;color:#667085;margin-top:5px}.notice{padding:10px 12px;border:1px solid #f4c46b;background:#fffaeb;color:#854a0e;border-radius:8px;margin-bottom:10px;font-size:13px}.toolbar{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-bottom:10px}.toolbar h2{font-size:18px;margin:0}.table-wrap{overflow:auto;border:1px solid #d7dfeb;border-radius:9px;max-height:66vh}.table{border-collapse:separate;border-spacing:0;min-width:1500px;width:100%;background:#fff}.table th,.table td{padding:9px 10px;border-right:1px solid #d7dfeb;border-bottom:1px solid #d7dfeb;text-align:left;font-size:12px;vertical-align:top}.table th{position:sticky;top:0;background:#e9eff7;z-index:3;white-space:nowrap;font-weight:800}.table tr:hover td{background:#f8fafc}.num{text-align:right!important;white-space:nowrap}.sub{display:block;color:#667085;font-size:10px;margin-top:3px}.dot{display:inline-block;width:8px;height:8px;border-radius:50%;background:#98a2b3;margin-right:5px}.dot.on{background:#12b76a}.filter-btn{margin-left:6px;border:1px solid #aab6c8;border-radius:5px;background:#fff;padding:1px 6px;cursor:pointer}.filter-btn.active{background:#155eef;color:#fff}.menu{position:fixed;z-index:100;background:#fff;border:1px solid #b9c4d4;border-radius:10px;box-shadow:0 14px 34px #0003;width:285px;padding:10px}.menu h4{margin:0 0 8px}.menu input[type=search]{width:100%;height:34px;border:1px solid #cbd5e1;border-radius:6px;padding:0 8px}.menu-list{max-height:240px;overflow:auto;border:1px solid #e2e8f0;border-radius:7px;margin-top:7px;padding:5px}.menu-list label{display:flex;gap:7px;align-items:center;padding:5px}.menu-actions{display:flex;gap:6px;justify-content:flex-end;margin-top:8px}.pager{display:flex;justify-content:flex-end;align-items:center;gap:8px;margin-top:10px}.empty{text-align:center!important;padding:30px!important;color:#667085}.source{display:inline-block;padding:3px 7px;border-radius:999px;background:#eff8ff;color:#175cd3;font-weight:700}.loading{opacity:.55;pointer-events:none}.account-strip{display:flex;gap:8px;flex-wrap:wrap;margin:8px 0}.account-chip{border:1px solid #d0d8e5;border-radius:8px;padding:7px 9px;background:#f8fafc;font-size:12px}.hidden{display:none!important}@media(max-width:1000px){.filters{grid-template-columns:repeat(2,minmax(180px,1fr))}.cards{grid-template-columns:repeat(2,minmax(160px,1fr))}}@media(max-width:600px){.filters{grid-template-columns:1fr}.wrap{padding:8px}.top{padding:0 8px}.tabs a{padding:7px;font-size:12px}}
</style>
</head>
<body>
<header class="top">
  <div class="brand">AIGUKA · Báo cáo V10</div>
  <nav class="tabs"><a data-view="dashboard">Hiệu quả QC</a><a data-view="daily">Báo cáo ngày</a><a data-view="leads">Khách hàng/Lead</a></nav>
  <button id="refresh" class="btn">↻ Làm mới</button>
</header>
<main class="wrap">
  <section class="panel">
    <div class="filters">
      <div class="field"><label>Từ ngày</label><input id="from" type="date"></div>
      <div class="field"><label>Đến ngày</label><input id="to" type="date"></div>
      <div class="field"><label>Trang Facebook</label><select id="page_id"></select></div>
      <div class="field"><label>Tài khoản QC</label><select id="ad_account_id"></select></div>
      <div class="field"><label>Chiến dịch</label><select id="campaign_id"></select></div>
      <div class="field"><label>Nhóm quảng cáo</label><select id="adset_id"></select></div>
      <div class="field"><label>Quảng cáo</label><select id="ad_id"></select></div>
      <div class="field"><label>Tìm kiếm</label><input id="search" placeholder="Tên, ID, SĐT, quảng cáo..."></div>
    </div>
    <div class="filter-actions">
      <div class="quick"><button class="btn" data-days="0">Hôm nay</button><button class="btn" data-days="7">7 ngày</button><button class="btn" data-days="30">30 ngày</button><button class="btn" id="month">Tháng này</button></div>
      <button id="reset" class="btn">Đặt lại</button><button id="apply" class="btn primary">Áp dụng bộ lọc</button>
      <label id="pageSizeWrap" class="field" style="margin-left:auto"><span style="font-size:12px;font-weight:700">Số dòng</span><select id="pageSize" style="width:90px;height:36px"><option>100</option><option selected>250</option><option>500</option><option>1000</option></select></label>
    </div>
  </section>
  <div id="cards" class="cards"></div>
  <div id="notice" class="notice">Đang tải nguồn dữ liệu…</div>
  <div id="accounts" class="account-strip"></div>
  <section class="panel">
    <div class="toolbar"><h2 id="title">Báo cáo</h2><div><button id="clearColumns" class="btn">Bỏ lọc cột</button> <button id="export" class="btn primary">Xuất Excel</button></div></div>
    <div id="tableWrap" class="table-wrap"><table class="table"><thead><tr id="head"></tr></thead><tbody id="rows"></tbody></table></div>
    <div id="pager" class="pager"></div>
  </section>
</main>
<div id="status" class="status">Đang kết nối dữ liệu…</div><div id="menuHost"></div>
<script>
(function(){
  const $=function(id){return document.getElementById(id)};
  const numberFormat=new Intl.NumberFormat('vi-VN',{maximumFractionDigits:0});
  const money=function(value){return numberFormat.format(Number(value||0))+' đ'};
  const esc=function(value){return String(value==null?'':value).replace(/[&<>"']/g,function(char){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]})};
  const localIso=function(date){const year=date.getFullYear(),month=String(date.getMonth()+1).padStart(2,'0'),day=String(date.getDate()).padStart(2,'0');return year+'-'+month+'-'+day};
  const today=function(){return localIso(new Date())};
  const daysAgo=function(days){const date=new Date();date.setDate(date.getDate()-days);return localIso(date)};
  const view=new URLSearchParams(location.search).get('view')||'dashboard';
  const S={filters:null,raw:[],count:0,accounts:[],columnFilters:new Map(),offset:0,loading:false,lastResponse:null};
  const definitions={
    dashboard:{title:'Hiệu quả quảng cáo · Meta trực tiếp',action:'ads',columns:[['ad_account_name','Tài khoản QC'],['campaign_name','Campaign'],['adset_name','Ad set'],['ad_name','Quảng cáo'],['effective_status','Trạng thái'],['spend_with_tax','Chi tiêu có VAT','money'],['impressions','Hiển thị','number'],['reach','Tiếp cận','number'],['clicks','Click','number'],['meta_conversations','Hội thoại Meta','number'],['conversations','Khách đối chiếu','number'],['contacts','SĐT/Zalo','number'],['contact_rate','Tỷ lệ','percent'],['cost_per_conversation','Giá/Hội thoại','money'],['cost_per_contact','Giá/SĐT','money']]},
    daily:{title:'Báo cáo theo ngày · Meta trực tiếp',action:'daily',columns:[['report_date','Ngày'],['page_name','Trang Facebook'],['ad_account_name','Tài khoản QC'],['payment_method_last4','Thẻ/nguồn tiền'],['data_status','Trạng thái dữ liệu'],['spend','Chi tiêu chưa VAT','money'],['tax_amount','VAT 5%','money'],['spend_with_tax','Chi tiêu có VAT','money'],['meta_conversations','Hội thoại Meta','number'],['conversations','Khách đối chiếu','number'],['contacts','SĐT/Zalo','number'],['message_count','Tin khách','number'],['contact_rate','Tỷ lệ','percent'],['cost_per_contact','Giá/SĐT','money']]},
    leads:{title:'Khách hàng / Lead',action:'leads',columns:[['customer_name','Khách hàng'],['phone','SĐT'],['zalo','Zalo'],['page_name','Trang Facebook'],['ad_account_name','Tài khoản QC'],['campaign_name','Campaign'],['adset_name','Ad set'],['ad_name','Quảng cáo'],['product_label','Sản phẩm'],['customer_source_type','Nguồn khách'],['pancake_employee','Nhân viên'],['last_snippet','Tin cuối'],['message_count','Số tin','number'],['last_customer_at','Thời gian','date']]}
  };
  const D=definitions[view]||definitions.dashboard;

  function setStatus(ok,text){const element=$('status');element.className='status '+(ok===true?'ok':ok===false?'bad':'');element.textContent=text}
  async function api(params){const response=await fetch('/functions/v1/aiguka-v8-report-api?'+params.toString(),{cache:'no-store'});const raw=await response.text();let data;try{data=JSON.parse(raw)}catch(_){data={error:raw.slice(0,500)}}if(!response.ok||data.ok===false)throw new Error(data.error||('HTTP '+response.status));return data}
  function option(value,label){return '<option value="'+esc(value||'')+'">'+esc(label||value||'')+'</option>'}
  function unique(items,key){return Array.from(new Map(items.filter(function(item){return item&&item[key]}).map(function(item){return [item[key],item]})).values())}
  function fillSelect(id,items,valueKey,labelKey,allLabel){const select=$(id),old=select.value;select.innerHTML=option('',allLabel)+items.map(function(item){return option(item[valueKey],item[labelKey]||item[valueKey])}).join('');if(Array.from(select.options).some(function(item){return item.value===old}))select.value=old;else select.value=''}
  function adsForAccount(){const account=$('ad_account_id').value;return (S.filters&&S.filters.ads||[]).filter(function(item){return !account||item.ad_account_id===account})}
  function rebuildCampaigns(){const ads=adsForAccount();fillSelect('campaign_id',unique(ads,'campaign_id'),'campaign_id','campaign_name','Tất cả chiến dịch');rebuildAdsets()}
  function rebuildAdsets(){const campaign=$('campaign_id').value;const ads=adsForAccount().filter(function(item){return !campaign||item.campaign_id===campaign});fillSelect('adset_id',unique(ads,'adset_id'),'adset_id','adset_name','Tất cả nhóm quảng cáo');rebuildAds()}
  function rebuildAds(){const campaign=$('campaign_id').value,adset=$('adset_id').value;const ads=adsForAccount().filter(function(item){return (!campaign||item.campaign_id===campaign)&&(!adset||item.adset_id===adset)});fillSelect('ad_id',unique(ads,'ad_id'),'ad_id','ad_name','Tất cả quảng cáo')}
  async function loadFilters(){const response=await api(new URLSearchParams({action:'filters'}));S.filters=response.data||{};fillSelect('page_id',S.filters.pages||[],'page_id','page_name','Tất cả Trang Facebook');fillSelect('ad_account_id',S.filters.ad_accounts||[],'ad_account_id','ad_account_name','Tất cả tài khoản');rebuildCampaigns()}
  function query(action){const params=new URLSearchParams({action:action||D.action,from:$('from').value,to:$('to').value});['page_id','ad_account_id','campaign_id','adset_id','ad_id','search'].forEach(function(id){if($(id).value)params.set(id,$(id).value)});if((action||D.action)==='leads'){params.set('limit',$('pageSize').value);params.set('offset',String(S.offset))}return params}
  function format(value,type,row,key){if(type==='money')return money(value);if(type==='number')return numberFormat.format(Number(value||0));if(type==='percent')return new Intl.NumberFormat('vi-VN',{maximumFractionDigits:2}).format(Number(value||0))+'%';if(type==='date'){if(!value)return '-';const date=new Date(value);return Number.isNaN(date.getTime())?esc(value):new Intl.DateTimeFormat('vi-VN',{timeZone:'Asia/Ho_Chi_Minh',hour:'2-digit',minute:'2-digit',day:'2-digit',month:'2-digit',year:'numeric'}).format(date)}if(key==='payment_method_last4')return value?'•••• '+esc(value):'-';if(key==='effective_status'){const active=/ACTIVE|ENABLED|RUNNING/.test(String(value||''));return '<span class="dot '+(active?'on':'')+'"></span>'+esc(value||'Không rõ')}if(key==='customer_source_type')return '<span class="source">'+(value==='comment'?'Bình luận Facebook':'Messenger')+'</span>';const idKey={ad_account_name:'ad_account_id',campaign_name:'campaign_id',adset_name:'adset_id',ad_name:'ad_id',page_name:'page_id',customer_name:'customer_id'}[key];return esc(value||'-')+(idKey&&row[idKey]?'<span class="sub">'+esc(row[idKey])+'</span>':'')}
  function filteredRows(){return S.raw.filter(function(row){for(const entry of S.columnFilters){const key=entry[0],selected=entry[1],value=String(row[key]==null?'(Trống)':row[key]);if(!selected.has(value))return false}return true})}
  function render(){const columns=D.columns,list=filteredRows();$('title').textContent=D.title;$('head').innerHTML='<th>#</th>'+columns.map(function(column){const key=column[0],label=column[1];return '<th>'+esc(label)+'<button class="filter-btn '+(S.columnFilters.has(key)?'active':'')+'" data-key="'+esc(key)+'">▾</button></th>'}).join('');if(!list.length)$('rows').innerHTML='<tr><td class="empty" colspan="'+(columns.length+1)+'">Không có dữ liệu phù hợp bộ lọc.</td></tr>';else $('rows').innerHTML=list.map(function(row,index){return '<tr><td class="num">'+(S.offset+index+1)+'</td>'+columns.map(function(column){const key=column[0],type=column[2],numeric=['money','number','percent'].includes(type);return '<td class="'+(numeric?'num':'')+'">'+format(row[key],type,row,key)+'</td>'}).join('')+'</tr>'}).join('');document.querySelectorAll('.filter-btn').forEach(function(button){button.onclick=function(event){event.stopPropagation();openColumnMenu(button.dataset.key,button)}});renderCards(list);renderPager()}
  function renderCards(list){let cards;if(view==='leads'){const customerKey=function(row){return String(row.page_id||'')+':'+String(row.customer_id||row.sender_id||'')};const uniqueCustomers=new Set(list.map(customerKey)).size,contacts=new Set(list.filter(function(row){return row.has_contact||row.phone||row.zalo}).map(customerKey)).size,comments=new Set(list.filter(function(row){return row.customer_source_type==='comment'}).map(customerKey)).size;cards=[['Khách đang hiển thị',uniqueCustomers,'Trang dữ liệu hiện tại'],['Tổng khách trong khoảng',S.count,'Có phân trang đầy đủ'],['Có SĐT/Zalo',contacts,'Giá trị đọc từ Core khi có'],['Khách comment',comments,'Trang dữ liệu hiện tại'],['Dòng sau lọc cột',list.length,'Bộ lọc đang áp dụng']]}else{const sum=function(key){return list.reduce(function(total,row){return total+Number(row[key]||0)},0)},spend=sum('spend_with_tax'),conversations=sum('conversations'),contacts=sum('contacts');cards=[['Chi tiêu có VAT',money(spend),'Meta trực tiếp'],['Hội thoại Meta',numberFormat.format(sum('meta_conversations')),'Meta Insights'],['Khách đối chiếu',numberFormat.format(conversations),'Reporting'],['Có SĐT/Zalo',numberFormat.format(contacts),'Core/Reporting'],['Giá/SĐT',contacts?money(spend/contacts):'-','Chi tiêu / liên hệ']]}$('cards').innerHTML=cards.map(function(card){return '<div class="card"><div class="label">'+esc(card[0])+'</div><div class="value">'+esc(card[1])+'</div><div class="hint">'+esc(card[2])+'</div></div>'}).join('')}
  function renderAccounts(){if(view==='leads'){$('accounts').innerHTML='';return}const list=S.accounts||[];$('accounts').innerHTML=list.map(function(account){return '<div class="account-chip"><b>'+esc(account.ad_account_name||account.ad_account_id)+'</b> · '+esc(account.currency||'')+(account.payment_method_last4?' · Thẻ •••• '+esc(account.payment_method_last4):' · Nguồn thanh toán chưa được Meta cấp quyền đọc')+'</div>'}).join('')}
  function renderPager(){const container=$('pager');if(view!=='leads'){container.innerHTML='';return}const size=Number($('pageSize').value),page=Math.floor(S.offset/size)+1,total=Math.max(1,Math.ceil(S.count/size));container.innerHTML='<button class="btn" id="prev" '+(S.offset<=0?'disabled':'')+'>← Trước</button><span>Trang '+page+' / '+total+' · '+numberFormat.format(S.count)+' dòng</span><button class="btn" id="next" '+(S.offset+size>=S.count?'disabled':'')+'>Sau →</button>';$('prev').onclick=function(){S.offset=Math.max(0,S.offset-size);load()};$('next').onclick=function(){S.offset+=size;load()}}
  function closeMenu(){$('menuHost').innerHTML=''}
  function openColumnMenu(key,button){closeMenu();const values=Array.from(new Set(S.raw.map(function(row){return String(row[key]==null?'(Trống)':row[key])}))).sort(function(a,b){return a.localeCompare(b,'vi',{numeric:true})}),current=S.columnFilters.get(key),menu=document.createElement('div');menu.className='menu';const column=D.columns.find(function(item){return item[0]===key});menu.innerHTML='<h4>Lọc: '+esc(column?column[1]:key)+'</h4><input type="search" placeholder="Tìm trong cột"><div class="menu-list">'+values.map(function(value){return '<label><input type="checkbox" data-value="'+esc(value)+'" '+(!current||current.has(value)?'checked':'')+'><span>'+esc(value)+'</span></label>'}).join('')+'</div><div class="menu-actions"><button class="btn" data-action="all">Chọn tất cả</button><button class="btn" data-action="clear">Bỏ lọc</button><button class="btn primary" data-action="apply">Áp dụng</button></div>';$('menuHost').appendChild(menu);const rect=button.getBoundingClientRect();menu.style.left=Math.max(8,Math.min(rect.left,innerWidth-300))+'px';menu.style.top=Math.min(rect.bottom+5,innerHeight-menu.offsetHeight-8)+'px';const search=menu.querySelector('input[type=search]');search.oninput=function(){menu.querySelectorAll('.menu-list label').forEach(function(label){label.classList.toggle('hidden',!label.textContent.toLowerCase().includes(search.value.toLowerCase()))})};menu.querySelector('[data-action=all]').onclick=function(){menu.querySelectorAll('.menu-list label:not(.hidden) input').forEach(function(input){input.checked=true})};menu.querySelector('[data-action=clear]').onclick=function(){S.columnFilters.delete(key);closeMenu();render()};menu.querySelector('[data-action=apply]').onclick=function(){const selected=new Set(Array.from(menu.querySelectorAll('.menu-list input:checked')).map(function(input){return input.dataset.value}));if(selected.size===values.length)S.columnFilters.delete(key);else S.columnFilters.set(key,selected);closeMenu();render()};search.focus()}
  function noticeFor(response){const warnings=Array.isArray(response.warnings)?response.warnings:[];let text;if(view==='leads'){text='Tên và SĐT/Zalo đọc trực tiếp từ Core; Reporting/Supabase cung cấp lịch sử, sản phẩm, nhân viên và phân trang. Không sao chép thêm số điện thoại thô sang kho báo cáo.';if(Number(response.core_enriched_count||0)>0)text+=' Đã ghép dữ liệu Core cho '+numberFormat.format(response.core_enriched_count)+' dòng trong trang này.'}else if(String(response.source||'').includes('meta'))text='Ngân sách và hiệu quả lấy trực tiếp từ Meta; Supabase chỉ đối chiếu khách và giữ dữ liệu dự phòng.';else text='Meta trực tiếp tạm chưa sẵn sàng; bảng đang dùng snapshot dự phòng từ Supabase.';if(warnings.length)text+=' Cảnh báo: '+warnings.map(function(item){return item&&item.error?item.error:String(item)}).join(' | ');return text}
  async function load(){if(S.loading)return;if(!$('from').value||!$('to').value){setStatus(false,'Thiếu khoảng ngày');return}if($('from').value>$('to').value){setStatus(false,'Khoảng ngày không hợp lệ');return}S.loading=true;$('tableWrap').classList.add('loading');setStatus(null,'Đang lấy dữ liệu…');try{const response=await api(query());S.raw=Array.isArray(response.data)?response.data:[];S.count=Number(response.count==null?S.raw.length:response.count);S.accounts=Array.isArray(response.accounts)?response.accounts:[];S.lastResponse=response;S.columnFilters.clear();render();renderAccounts();$('notice').textContent=noticeFor(response);setStatus(true,'Dữ liệu đã kết nối')}catch(error){setStatus(false,'Dữ liệu chưa kết nối');$('notice').textContent='Lỗi tải báo cáo: '+error.message;$('rows').innerHTML='<tr><td class="empty">'+esc(error.message)+'</td></tr>'}finally{S.loading=false;$('tableWrap').classList.remove('loading')}}
  function setRange(days){$('to').value=today();$('from').value=days===0?today():daysAgo(days-1);S.offset=0;load()}

  document.querySelectorAll('[data-view]').forEach(function(link){link.href='/dashboard?view='+link.dataset.view;link.classList.toggle('active',link.dataset.view===view)});
  $('pageSizeWrap').classList.toggle('hidden',view!=='leads');$('from').value=daysAgo(6);$('to').value=today();
  $('ad_account_id').addEventListener('change',function(){$('campaign_id').value='';$('adset_id').value='';$('ad_id').value='';rebuildCampaigns()});
  $('campaign_id').addEventListener('change',function(){$('adset_id').value='';$('ad_id').value='';rebuildAdsets()});
  $('adset_id').addEventListener('change',function(){$('ad_id').value='';rebuildAds()});
  $('apply').onclick=function(){S.offset=0;load()};$('refresh').onclick=load;
  $('reset').onclick=function(){['page_id','ad_account_id','campaign_id','adset_id','ad_id','search'].forEach(function(id){$(id).value=''});rebuildCampaigns();setRange(7)};
  $('month').onclick=function(){const date=new Date();$('from').value=localIso(new Date(date.getFullYear(),date.getMonth(),1));$('to').value=today();S.offset=0;load()};
  document.querySelectorAll('[data-days]').forEach(function(button){button.onclick=function(){setRange(Number(button.dataset.days))}});
  $('pageSize').onchange=function(){S.offset=0;load()};$('clearColumns').onclick=function(){S.columnFilters.clear();render()};
  $('export').onclick=function(){const params=query('export');params.set('report',D.action);location.href='/functions/v1/aiguka-v8-report-api?'+params.toString()};
  $('search').addEventListener('keydown',function(event){if(event.key==='Enter'){S.offset=0;load()}});
  document.addEventListener('click',function(event){if(!event.target.closest('.menu')&&!event.target.closest('.filter-btn'))closeMenu()});
  (async function(){try{await loadFilters();await load()}catch(error){setStatus(false,'Dữ liệu chưa kết nối');$('notice').textContent='Không tải được cấu hình báo cáo: '+error.message}})();
})();
</script>
</body>
</html>`;
}

export function installStableReportDashboard(app) {
  const send = (_req, res) => {
    res.setHeader("cache-control", "no-store, no-cache, must-revalidate, max-age=0");
    res.setHeader("content-type", "text/html; charset=utf-8");
    res.status(200).send(dashboardHtml());
  };
  app.get("/", send);
  app.get("/dashboard", send);
}

export const __private__ = { dashboardHtml };
